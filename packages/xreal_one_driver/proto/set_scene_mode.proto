syntax = "proto3";

package set_scene_mode;

message Value {
  // value of 0 is regular mode
  // value of 1 is some other mode with buttons disabled
  uint32 value = 1;
}

message Request {
  Value value = 3;
}

message Response {
  bytes data = 4;
}